# Copyright (c) 2001, Stanford University
# All rights reserved.
#
# See the file LICENSE.txt for information on redistributing this software.

import sys,os;
import cPickle;
import string;
import re;

sys.path.append( "../../opengl_stub" )
parsed_file = open( "../../glapi_parser/gl_header.parsed", "rb" )
gl_mapping = cPickle.load( parsed_file )

import stub_common;

keys = gl_mapping.keys()
keys.sort();



stub_common.CopyrightC()

print """
/* DO NOT EDIT - THIS FILE AUTOMATICALLY GENERATED BY pack.py SCRIPT */
#include <stdio.h>
#include "cr_string.h"
#include "cr_spu.h"
#include "replicatespu.h"
#include "cr_packfunctions.h"
#include "replicatespu_proto.h"
"""

num_funcs = len(keys) - len(stub_common.AllSpecials('replicatespu_unimplemented'))
print 'SPUNamedFunctionTable _cr_replicate_table[%d];' % (num_funcs+1)

print """
static void __fillin( int offset, char *name, SPUGenericFunction func )
{
	_cr_replicate_table[offset].name = crStrdup( name );
	_cr_replicate_table[offset].fn = func;
}"""

pack_specials = []

for func_name in keys:
	(return_type, args, types) = gl_mapping[func_name]
	if (return_type != 'void' or 
	    stub_common.FindSpecial( "replicatespu", func_name ) or 
	    stub_common.FindSpecial( "replicatespu_state", func_name ) or 
	    stub_common.FindSpecial( "replicatespu_get", func_name ) or 
	    stub_common.FindSpecial( "replicatespu_pixel", func_name ) or 
	    stub_common.FindSpecial( "replicatespu_flush", func_name ) or 
	    stub_common.FindSpecial( "replicatespu_client", func_name ) or 
	    stub_common.FindSpecial( "replicatespu_vertex", func_name ) or 
	    stub_common.FindSpecial( "../../packer/packer_get", func_name )):
	  pack_specials.append( func_name )

for func_name in keys:
	(return_type, args, types) = gl_mapping[func_name]
#	if stub_common.FindSpecial( "replicatespu_unimplemented", func_name ):
#		continue
	if func_name in pack_specials:
		print 'extern %s REPLICATESPU_APIENTRY replicatespu_%s%s;' % ( return_type, func_name, stub_common.ArgumentString( args, types ) )

print '\nvoid replicatespuCreateFunctions( void )'
print '{'
for index in range(len(keys)):
	func_name = keys[index]
	(return_type, args, types) = gl_mapping[func_name]
	if stub_common.FindSpecial( "replicatespu_unimplemented", func_name ):
		continue
	if func_name in pack_specials:
		print '\t__fillin( %3d, "%s", (SPUGenericFunction) replicatespu_%s );' % (index, func_name, func_name )
	else:
		print '\t__fillin( %3d, "%s", (SPUGenericFunction) (replicate_spu.swap ? crPack%sSWAP : crPack%s) );' % (index, func_name, func_name, func_name )
print '\t__fillin( %3d, NULL, NULL );' % num_funcs
print '}'
