TOP = ..

# The loader has to come first because it builds 
# the dispatch table header.  I know that sucks.

SUBDIRS = loader tester state_tracker
SUBDIRS += error nop pack passthrough render tilesort

include ${TOP}/cr.mk
