# Copyright (c) 2001, Stanford University
# All rights reserved
#
# See the file LICENSE.txt for information on redistributing this software.

import sys
sys.path.append( '../server' )
from mothership import *

if len(sys.argv) > 3 or len(sys.argv) < 2:
	print 'Usage: %s <demo>' % sys.argv[0] 
	sys.exit(-1)

CR_BIN_DIR = r'c:\work\cr\bin\WIN_NT'
demo = sys.argv[1]

server_spu = SPU( 'render' )
pack_spu = SPU( 'pack' )

server_spu.Conf( 'window_geometry', 100, 100, 500, 256 );

server_node = CRNetworkNode( )
server_node.AddSPU( server_spu )
server_node.FileClient( r'c:\tmp\dump' )

#client_node = CRApplicationNode( )
#client_node.AddSPU( pack_spu )
#pack_spu.AddServer( server_node, r'file://c:\tmp\dump' )
#pack_spu.AddServer( server_node, 'tcpip' )

#client_node.SetApplication( '%s/%s +devmap q3dm17' % (CR_BIN_DIR, demo) )
#client_node.StartDir( CR_BIN_DIR )

cr = CR()
cr.MTU( 32*1024 );
#cr.AddNode( client_node )
cr.AddNode( server_node )
cr.Go()
